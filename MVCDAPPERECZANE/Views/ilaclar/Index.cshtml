@model IEnumerable<MVCDAPPERECZANE.Models.ilaclarModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("ILAÇ EKLE", "EY")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ilacadi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ilacfiyat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ilacadi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ilacfiyat)
        </td>
        <td>
            @Html.ActionLink("DÜZENLE", "EY", new {  id=item.ilacNo  }) |
            @Html.ActionLink("SİL", "Delete", new {  id=item.ilacNo  })
        </td>
    </tr>
}

</table>
